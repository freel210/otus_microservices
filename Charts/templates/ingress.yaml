apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kuber-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: arch.homework
      http:
        paths:
        - path: /health
          pathType: Prefix
          backend:
            service:
              name: demo-service
              port:
                number: 8080
        - path: /swagger
          pathType: Prefix
          backend:
            service:
              name: gateway-service
              port:
                number: 5168
        - path: /user
          pathType: Prefix
          backend:
            service:
              name: demo-service
              port:
                number: 8080
        - path: /users
          pathType: Prefix
          backend:
            service:
              name: demo-service
              port:
                number: 8080
